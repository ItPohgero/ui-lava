import React from 'react';
import { Button } from 'antd';

const LavaButton: React.FC = () => (
    <div className="Button">
        <Button type="primary">Button</Button>
    </div>
);

export default LavaButton;